import 'package:flutter/material.dart';

class WorkshopsNotFound extends StatelessWidget {
  const WorkshopsNotFound();

  @override
  Widget build(_) => Center(child: Text('No se encontraron talleres'));
}
